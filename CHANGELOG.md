# 更新日志

## 3.6.1 - 2026-09-15

- 项目与 GitHub 仓库更名为 `vless-all-in`，移除原名称中的 `mlnb` 前缀。
- 同步更新安装器、自更新、升级脚本、Cloudflare Worker 示例和文档中的仓库地址。
- 增加静态检查，防止以后重新引入旧项目名。

## 3.6.0 - 2026-09-15

- 修复 AnyTLS 同时被注册为 Sing-box 与独立进程造成的安装和服务管理冲突。
- 自动迁移并统一 Telegram 配置，修复到期提醒读取旧配置的问题。
- 数据库改用同目录私有临时文件，菜单和 cron 写操作增加进程锁。
- 安装、自更新和升级流程增加 SHA256 与 Bash 语法校验。
- wgcf 使用上游官方校验和验证，下载失败时保留现有可执行文件。
- WARP 注册改用私有临时目录，私钥配置以 `600` 权限原子写入。
- 公网 IP 与地理位置查询改用 HTTPS。
- 改进真实证书的有效期、信任链和域名验证。
- 移除硬编码 ACME 邮箱，改为交互输入或 `ACME_EMAIL` 环境变量。
- 增加静态测试、GitHub Actions、换行规范、安全说明和运维文档。
